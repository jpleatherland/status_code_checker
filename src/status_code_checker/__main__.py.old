import sys

def main() -> int:
    if len(sys.argv) < 2:
        print("No arguments provided.")
        return 1

    statusCode: list[int] = parseCliArgs(sys.argv)

    print(statusCode)
    return 0

def parseCliArgs(args: list[str]) -> list[int]:
    result: list[int] = []
    for arg in args[1:]:
        try:
            result.append(int(arg))
        except (ValueError, TypeError):
            pass

    return result
